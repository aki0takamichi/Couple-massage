<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@drawable/couple_message_logo">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppThemeNoTitle" parent="AppTheme">
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
